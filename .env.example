# Copy this file's keys into your Xano workspace env vars
# (Dashboard → Settings → Environment Variables). Don't commit real values.

# Optional: default channel webhook. Functions also accept `webhook_url` as an input.
DISCORD_WEBHOOK_URL=
